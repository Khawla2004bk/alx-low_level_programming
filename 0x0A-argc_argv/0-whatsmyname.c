#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints its name, followed by a new line
 * @argc: num of arg
 * @argv: command line arg
 * Return: 0
 */

int main(int argc, char *argv[])
{
	pritf("%s\n", argv[argc * 0]);
	return (0);
}
